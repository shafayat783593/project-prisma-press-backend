enum ActiveStatus {
  ACTIVE
  BLOCKED
}

enum Role {
  USER
  ADMIN
}

enum PostStatus {
  DRAFT
  PUBLISHED
  AHCHIVED
}

enum CommentStatus {
  APPROVED
  REJECT
}

enum SubscriptionStatus{
    ACTIVE
    CANCLED
    EXPIRED
}
